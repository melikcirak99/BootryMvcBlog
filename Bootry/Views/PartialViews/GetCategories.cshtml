@model IEnumerable<Bootry.Models.Category>

<table class="table">
    <thead>
        <tr>
            <th style="width: 10px">#</th>
            <th>isim</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var cat in Model)
        {
            <tr>
                <td>@cat.ID</td>
                <td>@cat.Name</td>
            </tr>
        }
    </tbody>
</table>